import 'package:flutter/material.dart';

class BasicProfile extends StatelessWidget {
  const BasicProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Basic Profile Page'));
  }
}
